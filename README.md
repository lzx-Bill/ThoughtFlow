# ThoughtFlow - 想法卡片管理系统

一个活泼风格的想法卡片管理 Web 应用，支持卡片的创建、编辑、软删除和完整的编辑历史追溯。

## 技术栈

### 前端
- **React 18** - 组件化开发框架
- **Chakra UI** - 活泼风格 UI 组件库
- **Zustand** - 轻量级状态管理
- **Axios** - HTTP 请求库
- **Framer Motion** - 动画库

### 后端
- **FastAPI** - 高性能异步 Python Web 框架
- **Motor** - 异步 MongoDB 驱动
- **Pydantic** - 数据验证

### 数据库
- **MongoDB** - 非关系型数据库（远程服务器：192.168.31.233:27017）

## 项目结构

```
ThoughtFlow/
├── backend/                 # Python FastAPI 后端
│   ├── app/
│   │   ├── __init__.py
│   │   ├── main.py          # FastAPI 应用入口
│   │   ├── config.py        # 配置文件
│   │   ├── database.py      # MongoDB 连接
│   │   ├── models/          # 数据模型
│   │   │   ├── __init__.py
│   │   │   └── idea_card.py
│   │   ├── schemas/         # Pydantic 模式
│   │   │   ├── __init__.py
│   │   │   └── idea_card.py
│   │   └── routers/         # API 路由
│   │       ├── __init__.py
│   │       └── idea_cards.py
│   ├── requirements.txt
│   └── .env.example
├── frontend/                # React 前端
│   ├── src/
│   │   ├── api/             # API 请求
│   │   ├── components/      # 组件
│   │   ├── stores/          # Zustand 状态管理
│   │   ├── types/           # TypeScript 类型
│   │   ├── App.tsx
│   │   └── main.tsx
│   ├── package.json
│   └── vite.config.ts
└── README.md
```

## 快速开始

### 方式一：一键启动（推荐）

**Windows 批处理脚本：**
```bash
start.bat
```

**PowerShell 脚本：**
```bash
.\start.ps1
```

### 方式二：手动启动

**1. 启动后端（端口 13089）**
```bash
cd backend
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
uvicorn app.main:app --host 0.0.0.0 --port 13089 --reload
```

**2. 启动前端（端口 3089）**
```bash
cd frontend
npm install
npm run dev
```

**3. 访问应用**
- 前端：http://localhost:3089
- 后端 API：http://localhost:13089
- API 文档：http://localhost:13089/docs

### 停止服务

```bash
stop.bat
```

## API 接口

| 方法 | 路径 | 描述 |
|------|------|------|
| POST | /api/idea-card | 新建卡片 |
| GET | /api/idea-cards | 查询所有正常卡片 |
| GET | /api/idea-cards/deleted | 查询所有已删除卡片 |
| PUT | /api/idea-card/{card_id} | 编辑卡片 |
| PATCH | /api/idea-card/{card_id}/delete | 逻辑删除卡片 |
| PATCH | /api/idea-card/{card_id}/recover | 恢复已删除卡片 |
| GET | /api/idea-card/{card_id}/history | 查询编辑历史 |

## 核心特性

- 🎨 **活泼风格** - 马卡龙配色、大圆角、柔和阴影
- 📝 **卡片化布局** - 瀑布流展示，无导航栏干扰
- ✏️ **原地编辑** - 点击卡片直接进入编辑态
- 🗑️ **软删除** - 删除仅隐藏，数据完整保留
- 📜 **编辑历史** - 完整记录每次修改的新旧值
- 🔄 **恢复功能** - 已删除卡片可随时恢复

## 许可证

MIT License
